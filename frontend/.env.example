# Optional public API base URL.
# Leave empty for same-origin API calls with Next.js rewrite.
NEXT_PUBLIC_API_BASE_URL=

# Preferred server-side backend origin used by Next.js rewrites.
# Set this when backend is hosted separately.
BACKEND_API_ORIGIN=http://127.0.0.1:8000
